<%= form_for(@milestone_type) do |f| %>
  <% if @milestone_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone_type.errors.count, "error") %> prohibited this milestone_type from being saved:</h2>

      <ul>
      <% @milestone_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :milestone_type_name %><br>
    <%= f.text_field :milestone_type_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
